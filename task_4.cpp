#include <chrono>
#include <random>
#include "TaskTimer.h"

using namespace std;
using namespace std::chrono;
/*
int main() {
    random_device rd;
    mt19937 gen(rd());

    vector <int> array;
    for (int i = 0; i < 100000; i++) {
        array.push_back(gen());
    }

    TaskTimer::measureSorting(array);
} */